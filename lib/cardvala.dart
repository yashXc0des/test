import 'package:flutter/material.dart';
class cardvalacode extends StatelessWidget {
  const cardvalacode({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Card(
          child: Container(
            child: Text("TESTING VALA CODE , HELLO CODDER",style: TextStyle(fontSize: 30),),
          ),
        ),
      ),
    );
  }
}
